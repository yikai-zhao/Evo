services:
  - type: web
    name: evo
    runtime: node
    buildCommand: npm install --omit=dev
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
